# CURRENT-PROGRESS — Colar de Sons

**Authoritative progress lives in Linear** (project Sound Necklace, milestone MVP —
issue states + the blockedBy graph decide what is next; see APPLICATION-SPEC.md).
This file is an **append-only iteration log** for fast warm-up: skim it, then go to
Linear. One line per loop iteration, newest last:

`YYYY-MM-DD HH:MM · ENG-### · result (merged | in-review | blocked: reason) · PR #`

## Log

- 2026-07-09 · (pre-loop) · Planning session complete: 47 issues ENG-212…ENG-258 created; repo skeleton + CI gates + docs/architecture.md delivered · PR #1 (skeleton, awaiting human merge)
- 2026-07-09 03:55 · ENG-214 · in-review (contract-critical: domain grid/hash/ids + golden cases 1 e 3 byte-idênticos; aguarda revisão humana) · PR #6
- 2026-07-09 09:15 · ENG-216 · in-review (contract-critical: domain state/frontier/selection/scenes + replay dos passos de cena do golden case 2; coverage domain 100%; aguarda revisão humana) · PR #10
- 2026-07-09 10:20 · ENG-215 · merged (ui/atoms: 7 átomos Shemá, 45 testes dom, guarda reduced-motion + minimalismo §9.2) · PR #11
- 2026-07-09 10:55 · ENG-217 · merged (adapters/audio: porta AudioEngine + núcleo de playback 1:1 da referência com transport injetado; fixture LCG headless; Web Audio real com smoke skipIf; 31 testes) · PR #12
- 2026-07-09 15:45 · ENG-218 · merged (ui/molecules: 10 moléculas Shemá — bead-row, selection-band, scene/phrase chip, confidence-trio radiogroup+roving, kind-card, question/document cards, stepper-station, progress-dots, trust-chip; 45 testes dom + guarda minimalismo §9.2; a11y APG/WCAG) · PR #14
- 2026-07-09 16:20 · ENG-220 · merged (ui/organisms: o colar — geometria pura portada 1:1 da referência (janela/beadAtXY/drawBand), modelo de clique delegado com 1 listener nativo, hover-edge dwell 280ms, head-tap, windowing por cena com dim+banda tracejada, banda de seleção com bordas, iluminação de playback IMPERATIVA via data-play sem re-render; 30 testes incl. 9 de interação em Chromium real + guarda §9.2 + guarda reduced-motion) · PR #15
- 2026-07-09 16:50 · ENG-219 · in-review (contract-critical: domain triagem — scene-kinds (27 = 19 ALTA + 8 comum, SK_PT, skShort, T_TARGET), triagem (tagScene/markNoneFit + selectors lockedParts/productiveScenes), coverage (firme/hesitante, candidato a ausência, trava all-none-fit), gates (triagemDone + copy §8.5 verbatim, modeLocks, resolveMode redirect só-produtiva, setMode + efeito partsConfirmed); replay de triagem do golden case 2 (pending avança p/ phraseSelect/ENG-223); domain 100% linhas/100% funcs/96.75% branch; aguarda revisão humana) · PR #TBD
- 2026-07-09 18:05 · ENG-221 · merged (ui/organisms: palco da conversa + guia contador — conversation-stage reusa QuestionCard/BeadRow/WaveformBar/Button, gravador dirigido por props (idle→recording→recorded) com callbacks, fio de progresso sem dígitos §9.2, canal digitado opcional; storyteller-guide com mecanismo de variante por import.meta.glob (prefere animated, traz só static SVG humano), reduced-motion gated; 12 testes dom + 2 de interação em Chromium real) · PR #17
- 2026-07-09 18:50 · ENG-224 · merged (ui/app shell — roteador History-API (useSyncExternalStore, sem react-router), 3 registries add-a-file por import.meta.glob (estações/adapters/addons) + station-host fallback "em construção", fio de contas derivado de modeLocks (clique delegado só em alcançável, §9.2), player itinerante único (portal p/ nó persistente movido por appendChild, stop() na troca), chrome de review/lock (§8.10/§7.3), connection gate online-only (adapters/connectivity fixture + organismo) + ui/state bridge Zustand (apply guardado por canEdit=online&&!review&&!lock, autosave injetável, som mudo); 42 testes dom/unit) · PR #18
- 2026-07-09 19:55 · ENG-230 · merged (ui/pages — Escuta 2: corte de cenas. Palco creme, colar com ancoragem ativa (segments/lockedEndBeads/selection memoizados por `session.parts` p/ não recomputar no playbackHead), modelo de clique delegado ao `clickBead` do domínio com áudio na hora via `playActionOn` puro (single-bead/range→`play`, edge→`playEdge`), ação dominante "✓ Confirmar esta cena"→`confirmPart` (trava span, conta final quadrada, próxima cena auto-abre na emenda), chips `ScenePhraseChip` com Reabrir→`reopenPart` (cascata destrava i..fim), "Confirmar as cenas →" só com ≥1 travada→`confirmParts`→Triagem, "← Voltar" = port do cenasBack (`setMode({...s,whole:{confirmed:false}},'escuta')`, cenas preservadas), cópias de erro exatas do domínio, rótulo de cena por extenso PT-BR (`sceneLabel`) p/ tela digit-free §9.2, movimento sob reduced-motion; áudio injetado por prop `player`; 8 unit + 10 dom + 1 browser Chromium) · PR #TBD
- 2026-07-09 21:05 · ENG-223 · in-review (contract-critical: domain frases/costura — phrases.ts (fronteira com back-reach sem clamp, confirmFrase sem checar pendingStart, cascata GLOBAL do reopen com flagged sobrevivente, P# dangling, cursor 1º/último destravado assimétrico, enterSegmentacao, confirmFrasesDone com marcador warnedEmptyScene como dado) + seam.ts (vizinhas travadas, classifyBorderMove puro simple/escalation/two-productive com limiar max(3,round(25%)) e consumed, slideSeam imutável, janela §8.6); replayer do golden ganha os 7 passos de frase (borderDecision, toggleFlag sobre travadas, sceneDone forceEmpty) e minimal-flow para pendente em answer/12 com mode=mapeamento; domain 100% linhas/97.6% branch; aguarda revisão humana) · PR #21
- 2026-07-10 01:25 · ENG-227 · in-review (contract-critical: contracts/ real — serialize.ts (serializador único JSON.stringify(x,null,2) sem newline, filenames com fallback "colar" só de arquivo), manifesto.ts (ManifestoSchema strictObject + buildManifesto 1:1 L1316–17 + gate silencioso !totalBeads), retorno.ts (RetornoSchema com enum dos 27 kinds derivado de SCENE_KINDS + buildRetorno 1:1 L1318–29: S# sequencial só de travadas, ||null, flags independentes de locked, propositions em ordem de criação global + retornoExportStatus com semFim L1335); fixtures válidas/inválidas por schema; golden: casos 1/3 agora exportam pelos mappers REAIS e os 2 JSONs do caso 2 byte-idênticos no registry.test (caso 2 segue pendente no golden.test até ENG-226/233); contracts 100% cobertura; aguarda revisão humana) · PR #23
- 2026-07-09 19:30 · ENG-229 · merged (ui/pages — PRIMEIRA estação: Escuta 1 cerimonial. Colar `transportOnly`; transporte em `transport.ts` puro (botão grande = chave estável `historia` p/ pausar no 2º toque; cada toque de conta = chave nova → sempre reinicia dali; `onHead` re-alterna o último = pausa/retoma), `playbackHead` via useEffect+onHead com `stop()` no unmount; decisão única `confirmWhole`/`reopenWhole` por `sessionStore.apply`, cópia de erro exata do domínio no span incompleto; tratamento full-bleed olive + tagline Merriweather itálico + marca-d'água + movimento sob guarda reduced-motion; minimalismo §9.2 (0 dígitos, ≤1 instrução, 1 ação dominante); áudio injetado por prop `player` — dormente até o Setup/ENG-243; 11 dom/unit + 2 browser Chromium) · PR #19
- 2026-07-10 03:05 · ENG-231 · merged (ui/organisms + ui/app/addons: tutorial popup da facilitadora — organismo sobre @radix-ui/react-popover SEM Portal (conteúdo inline p/ ficar DENTRO da .cds-addons-layer, nunca no main da estação; Popper posiciona fixed mesmo inline), dica PT-BR curta por estação, auto-open sem roubar foco (onOpenAutoFocus prevenido), dois níveis de dismiss: X/ESC/fora = sessão (useState no addon montado entre estações), "Não mostrar de novo" = localStorage `colar-de-sons:tutorial:dismissed:v1` (1º uso de localStorage no repo; get/set em try/catch com degradação silenciosa; chave documentada no README do organismo), trigger "?" permanece como rota de reencontro; addon ui/app/addons/tutorial.tsx (1º do glob, zero edição no shell) deriva a estação via stepperStations; gotcha: o glob de addons come qualquer .tsx irmão → testes do addon em `addons/__tests__/`; registries.test "começa vazio" substituído por descoberta real do glob (emenda mínima fora do Scope, exigida pelo CI verde); 15 testes dom no organismo + 3 no addon) · PR #26
- 2026-07-10 02:30 · ENG-228 · merged (ui/organisms: seam modal — diálogo de travessia de borda sobre @radix-ui/react-dialog (1ª dep Radix; comportamento only, visual 100% Shemá do protótipo 1g): conjuntos de opções EXATOS por variante da oferta do domínio (simple/escalation±canMove/two-productive), ESC+overlay→onReanchor via onOpenChange (default seguro §9.5), preview antes/depois da costura ("borda de hoje"/"borda nova") com slide só sob no-preference, digit-free §9.2 (question/warning do domain com dígitos NÃO renderizados — delta/thr internos; "Reancorar dentro da cena" do PRD v2 vence "Ficar dentro da cena" do protótipo), ghost do Button recolorido por override escopado p/ painel olive, foco inicial no Reancorar (APG, Enter acidental nunca move borda), janela do trecho sempre inclui a borda nova (consumido além da vizinha) + centragem por scrollLeft; 22 dom + 3 Chromium (focus trap real; dispensa de overlay = pointerdown+click por deferPointerDownOutside)) · PR #25
- 2026-07-10 07:45 · ENG-225 · merged (ui/organisms: Triagem picker + coverage drawer — ADOÇÃO de trabalho órfão: a sessão de 2026-07-09 21h48 morreu sem push; esta iteração recuperou os 2 commits, rebaseou sobre a main pós-ENG-228/231 e reconciliou a dep Radix (pacote unificado radix-ui → @radix-ui/react-dialog já presente, import * as Dialog). Picker presentacional §8.5: "Mais comuns" = 8 kinds do tier comum, disclosure "Ver todos os tipos por tema" (6 temas display-only no organismo, tints scenePalette[0..5]), "Nenhum se encaixa" persistente, filtro sem acentos (PT-BR+EN, sem contagem visível — §9.2), escolher tipo troca a grade pelo passo de confiança (chip+"trocar tipo"+ConfidenceTrio) emitindo onConfirm(kind, alta|média|baixa)/onNoneFit; radiogroup único com roving via querySelectorAll (KindCard sem ref), setas movem foco sem marcar (variante toolbar APG). Drawer "Cobertura · só facilitadora" sobre Radix Dialog: painel olive 340px, fechado por padrão, linhas mono firme/hesitante/alvo ("1–2" ALTA é exibição do drawer), chips candidatos a ausência em PT; barrel intocado (escopo). 28 testes dom + guarda §9.2. Também nesta iteração: squash-merge do PR #26 órfão (ENG-231, verde desde ~06h20) e ENG-222 devolvida a Todo com nota do worktree órfão) · PR #27
- 2026-07-10 00:55 · ENG-226 · in-review (contract-critical: domain roteiros do mapeamento — mapeamento-scripts.ts (L1_Q 11/L2_Q 5/L3_Q 5 verbatim L1030–1056, byte-testados contra a referência extraída em runtime + guarda NFC; quirks field vazio/ausente) + mapping.ts (ensureMapping L1057–1063 com extensão preguiçosa `== null`, L2 incl. none_fit, L3 por frase travada c/ part_link de qualquer cena, nunca apaga; setAnswer com atribuição do driver; productiveFrases L1082 cena-major; questionSequence §8.7; voiceAnswerPath §10.4/O5); registry consome passos answer → minimal-flow pendente em export/17; domain 100% linhas/97.7% branch; aguarda revisão humana) · PR #37
- 2026-07-10 00:55 · ENG-226 · in-review (contract-critical: domain roteiros do mapeamento — mapeamento-scripts.ts (L1_Q 11/L2_Q 5/L3_Q 5 verbatim L1030–1056, byte-testados contra a referência extraída em runtime + guarda NFC; quirks field vazio/ausente) + mapping.ts (ensureMapping L1057–1063 com extensão preguiçosa `== null`, L2 incl. none_fit, L3 por frase travada c/ part_link de qualquer cena, nunca apaga; setAnswer com atribuição do driver; productiveFrases L1082 cena-major; questionSequence §8.7; voiceAnswerPath §10.4/O5); registry consome passos answer → minimal-flow pendente em export/17; domain 100% linhas/97.7% branch; aguarda revisão humana) · PR #22
- 2026-07-10 12:15 · ENG-233 · in-review (contract-critical: contracts/relatorio.ts — buildMapReport port 1:1 de buildMapReportMd (L1155–1170): esqueleto byte-exato (título travessão U+2014; contexto com pontos médios U+00B7 + manifest; N1 duas linhas c/ `_(field)_` e resposta indentada 2 espaços; N2 por cena travada incl. none_fit → `(nenhum) [none_fit]`; N3 não-bold, `**Frase i (Pn) — contas s–e:**` com U+2014 antes de "contas" e U+2013 entre as contas; `_(sem resposta)_` em falta; `sceneNum`=indexOf lockedParts → S# com lacuna; push final = 1 newline). Extensão de voz PRD §10.4 (2º param `voice: Set<path>`; digitado>voz(`voiceAnswerPath`)>sem-resposta; digitado vence; vazio ⇒ byte-idêntico à referência) c/ fixture PRD-derived + README. `relatorioFilename` fallback "historia" SEM acento (≠ "colar" dos JSONs, ≠ título "história"). Golden: novo `sessionExportReplayer` compõe sobre replaySessionSteps (inalterado) e exporta os 3 artefatos pelos mappers reais → minimal-flow SAI de pendente, byte-diff dos 3 a cada CI. contracts 100% linhas / 96% branch da camada; typecheck/lint/depcruise/golden verdes; aguarda revisão humana) · PR #TBD
- 2026-07-10 04:55 · ENG-269 · in-review (contract-critical: fiação do phraseFrontier — frontier(state,'frases') ganha o ramo de cena ativa da referência L400–409 (última frase travada NA cena +1; back-reach ao início da vizinha anterior; sem clamp em totalBeads−1), herdado por activeAnchor e pelo clamp do clickBead; activeScene movida phrases.ts→seam.ts p/ quebrar ciclo (grafo frontier→seam→triagem→state, depcruise sem-ciclos verde), phraseFrontier vira alias e a API pública não muda; 9 testes novos (frontier clamp/back-reach/quirk sem-cap + clickBead camada de frases discriminante cena vs global); golden byte-idêntico (replay não usa clickBead); domain 100% linhas / 97.6% branch; aguarda revisão humana) · PR #24
- 2026-07-10 11:55 · ENG-235 · in-review (contract-critical: contracts/ API+bucket PROVISÓRIOS — bucket.ts (AcoustemeEnvelope strictObject{version,data:unknown} opaco §15.2 O8, GranularityLevel pequena/media/grande §8.1, BucketAudio c/ consent_present §12/O6 + acousteme nullable §6.1, BucketListResponse) + api.ts (auth login/refresh/token/me + RoleSchema facilitator/project_admin §7.1; sessions status em_progresso/concluida, SessionStep 6 estações, SessionSummary/List, CreateSession c/ pipeline_consent, autosave = looseObject{schema_version} opaco deixando o session-state real p/ ENG-234, AutosaveResponse; artifacts OpaqueArtifact=z.string() §10.5 + ArtifactKind/Triple + CompleteSession; LockStatus/Holder §7.3/O4; ResourcePath regex 3 formas §10.4/O5); custódia opaca provada por parse-identidade + grep sem JSON.parse(/stringify( no módulo; fixture bucket-list.json (3 áudios, acousteme com/sem, consent variado) p/ adapters do ENG-241 + README PRD-derivado; 44 testes (valid/invalid por surface), contracts ≥90%, typecheck/lint/depcruise/golden verdes; aguarda revisão humana) · PR #TBD
- 2026-07-10 14:20 · ENG-234 · in-review (contract-critical: contracts/session-state.ts — DTO de autosave versionado `schema_version:1` com `toSessionDto`/`fromSessionDto` round-trippando TODO o `SessionState` do domínio + `SessionMeta` fora do domínio (granularidade §8.1, áudio do bucket §7.4, consentimento pipeline §12/O6, refs de voz §10.4/O5); `strictObject` (dado validado na leitura §10.5), `whole.id` string no DTO (cast p/ `'S1'` na volta), transientes `warnedEmptyScene`/`mapStep` NÃO persistidos. contracts/imports.ts — `applyDelivery`/`applyReturn` ports 1:1 da referência (L1341–1383): entrega DESTRAVADA c/ prefills+fallbacks PT#/P# e predicado de mismatch não-bloqueante; retorno TRAVADO c/ partsConfirmed + flags por prop_id + cursor→frases + sem checagem de manifest; ambos recusam grade ausente (`{ok:false,reason:'no-grid'}`), cópias PT-BR expostas p/ ENG-248; `z.object` leniente (fiel à referência). Passo `importReturn` no golden registry (habilita o caso import-return-roundtrip da ENG-238) + prova de fidelidade import→export byte-idêntica no registry.test/imports.test. Fixtures válidas/inválidas por schema; contracts 100% linhas / 95.27% branch; typecheck/lint/depcruise/golden verdes; aguarda revisão humana) · PR #TBD
- 2026-07-10 15:35 · ENG-237 · merged (ui/pages — estação Segmentação: as frases dentro de uma cena. Palco creme, título "Cena N · <tipo>" por extenso, colar EM JANELA na cena produtiva ativa (`window={activeScene.span}` → organismo faz margem `max(3,round(2/beadSec))`+dim+banda tracejada), modelo de clique delegado ao `clickBead` com áudio na hora, ação dominante "✓ Confirmar esta frase"→`confirmFrase` (união discriminada locked/border/noop/error); travessia de borda abre o seam-modal com `result.offer` e as decisões `moveBorder`(desliza a costura+trava)/`reanchorFrase`(limpa)/`setMode('triagem')`(escala), vizinha do lado da travessia por `nextNeighbor`/`prevNeighbor`; chips de frase travada (swatch·"Frase N"·▶·Reabrir·⚑ revisar/⚑ marcada·Remover) por `reopenFrase`/`toggleFlag`/`removeFrase`; "▶ ouvir a cena" toca só o span; cena-vazia avisa uma vez (`confirmFrasesDone` com marcador UI-local `warnedEmptyScene`), 2º clique segue; "← Voltar"=cena produtiva anterior(`enterScene`) ou Triagem na 1ª; última cena→Mapeamento; cópias PT-BR verbatim do domínio (`FRASE_ERROR_COPY`/`FRASES_EMPTY_WARNING`), minimalismo §9.2 (0 dígitos exceto a conta na linha de erro do facilitador). Reusa `playActionOn`/`sceneColor`/`sceneLabel` da Escuta 2 (irmã, sem duplicar o cardinal); `phraseLabel`="Frase N" por extenso, cor por `phrasePalette`. Um único `useMemo` keyed em `[session]` p/ estabilidade de ref na iluminação (evita o ERRO do React Compiler `preserve-manual-memoization`). 25 unit/dom + 1 browser Chromium; typecheck/lint(0 erros)/depcruise/test(715)/test:browser(19)/golden(18/18) verdes) · PR #34
- 2026-07-10 14:50 · ENG-238 · in-review (contract-critical: golden harness — pacote de 14 casos de borda + `STRICT = true`. Cada caso = script de decisão + goldens gerados da referência intocada. `reopen-cascade` (cascata destrava i..fim, re-corte, PT# estável PT1-3 + S# reatribuído em ordem), `pid-reuse` (remove P2 do meio → nova frase reusa o menor P# livre; ordem final P1,P3,P4,P2), `back-reach` (fronteira da 1ª frase = início da vizinha anterior; cruza o início → costura desliza, PT1 encolhe a 0-9), `seam-small-move`/`seam-large-move` (fim-cruza delta ≤/> max(3,25%): "mover a borda" / "mover mesmo assim", vizinha encolhe), `seam-escalation` (vizinha produtiva COM frases → escalada de 2 cenas sem "mover", reancora dentro), `seam-consumed` (seleção engole a vizinha → sem "mover", reancora), `all-none-fit` (todas none_fit → scene_kind null em todas + propositions []), `empty-scene-report` (2º clique atravessa o aviso → Mapeamento; relatório N3 sem blocos de Frase + retorno propositions []), `flag-no-proposition` (frase marcada+reaberta → flags com entrada sem proposition), `mapping-lazy` (reabre+re-corta+nova frase → respostas antigas por prop_id sobrevivem, novas semeadas), `hash-variants` (PCM multicanal, sr 22050, N>100k stride 2), `exact-multiple` (dur múltiplo exato de beadSec → sem conta parcial), `import-return-roundtrip` (semeia por retorno → re-exporta byte-idêntico). Passo `importReturn` novo no `generate.mjs` espelha o handler de retomada da referência (L1362–1383); `registry.ts` registra os 14 replayers (manifest/sessionExport) e liga `STRICT=true` — caso sem replayer agora REPROVA. NENHUMA divergência de domínio: os 14 reproduzem byte-idêntico por domain+contracts (`pnpm golden` 18/18 STRICT verde) e batem com a referência fresca (`golden:verify` verde). CI `golden-harness` já roda strict+verify. typecheck/lint/depcruise verdes. Escopo: só tests/golden/** (+ noridocs). NÃO mergear — aguarda revisão humana) · PR #TBD
- 2026-07-10 15:22 · ENG-240 · merged (adapters/sessions: porta SessionStore fixture-safe/loop-ready — só adapters/. `createAutosaver` reusado pela fixture e pelo HTTP: debounce+coalescing (Map, último estado vence), retry com backoff exponencial e PAUSA offline via ConnectivityMonitor (flush no reconnect, zero perda §13); guarda de reentrância no drain + `cancel(id)` (complete/reopen cancelam pendente) + flush re-arma outras sessões da janela. `FixtureSessionStore` sobre `FixtureSessionBackend` PARTILHÁVEL (servidor simulado → 2 stores/1 backend = 2 usuários, exercita o lock consultivo que NUNCA lança em conflito → não-holder abre em revisão §7.3); persistência opcional via `KeyValueStorage`/localStorage (chave `colar-de-sons:sessions:v1`, bytes de voz só em memória); passo do dashboard derivado do `mode` (`stepFor`). Custódia OPACA §10.5 por `structuredClone` (estado+artefatos byte-idênticos; artefato = string imutável). `HttpSessionStore` esqueleto real contra contracts/api.ts com `fetch` injetado (sem rede no CI), endpoints provisórios até ENG-211/247. register `{port:'sessions',fixture,real}`. 18 testes unit (coalesce/pausa+reconnect/retry/flush-multi/cancel; ciclo de vida; byte-identidade de artefato; lock 2-usuários; recursos put/get/list; persistência de reload); code-review incorporada. typecheck/lint/depcruise/golden(18 STRICT)/test(708) verdes) · PR #33
- 2026-07-10 17:22 · ENG-244 · merged (adapters/voice: porta VoiceRecorder fixture-safe/loop-ready — só adapters/voice/. Port `start(path)→Recording{onLevel/stop/cancel}`, `play/stopPlayback/has/delete`; persistência desacoplada por `VoiceResourceStore` (put/get/has/delete) — fixture/registry usam `MemoryVoiceStore` (cópias defensivas §10.5). `path.ts` REUSA `voiceAnswerPath` do domínio + valida com `ResourcePathSchema` dos contracts (não reinventa o caminho §10.4/O5). `FixtureVoiceRecorder` headless: níveis LCG determinísticos avançados por hook `tick()` (emitem só durante a gravação), blob WebM estático; `start` devolve `FixtureRecording` (retorno covariante p/ o `tick`). `WebVoiceRecorder` real sobre MediaRecorder+Web Audio: `audio/webm;codecs=opus` feature-detect→`VoiceUnsupportedError`, getUserMedia negado→`MicPermissionError`, metering RMS via AnalyserNode, playback por `<audio>`; TODAS as deps de plataforma injetáveis → caminhos de erro + gravar→parar→persistir testados em node sem microfone (FakeMediaRecorder, sem AudioContext p/ evitar rAF). FOLLOW-UP p/ ENG-249: SessionStore não expõe `deleteResource` — ligar o store real por sessão + adicionar delete fica lá; registry usa MemoryVoiceStore como placeholder. 20 testes unit; typecheck/lint(0 erros)/depcruise(268 mód, sem violação)/test(cobertura domain 100%/contracts 100%)/golden(18/18) verdes) · PR #TBD
- 2026-07-10 17:05 · ENG-236 · merged (ui/pages — estação Triagem: classificar cada cena travada. Redesign §6.4 reformata a lista vertical da referência em UMA cena em foco + `ProgressDots` (atalho de salto, digit-free); "▶ Ouvir esta cena" toca o span pelo player; estado atual sempre visível (`tagShow`: por classificar / `skShort · confLabel` / ⌀ nenhum se encaixa); `TriagemPicker` (keyed por part_id → reseta por cena) emite `onConfirm`→`tagScene`/`onNoneFit`→`markNoneFit`, seguido de auto-avanço p/ a próxima pendente (`_nextPending` do protótipo). Gate duro "Já classifiquei todas as cenas →" por `triagemDone` (habilita só com todas não-pendentes E ≥1 produtiva), cópia de ajuda verbatim do domínio; avança compondo `setMode(s,'segmentacao')`+`enterSegmentacao` (só quando modo efetivo=segmentacao, port do L1006–1008 → estação Segmentação não abre nula). none-fit finding (frase-contrato "evidência para nomear um tipo nativo…") + lockout all-none-fit (Segmentação/Mapeamento travados, digit-free). `CoverageDrawer` só-facilitadora auto-contida (Radix Dialog fechado por padrão → nada dela no DOM/§9.2 em repouso). Organismos importados por caminho direto (barrel congelado ENG-225; depcruise sem-ciclo verde). Sem browser test (Triagem não tem `Necklace`/geometria). 11 dom; typecheck/lint(0 erros)/depcruise/test(744)/test:browser(19)/golden(18/18) verdes) · PR #TBD
- 2026-07-10 17:20 · ENG-222 · merged (ui/organisms — dashboard: session-list + artifact-cards. ADOÇÃO de trabalho órfão: a sessão de 2026-07-09 21h26 morreu sem push (2 branches locais `eng-222-orphan-*` e `henok/eng-222-*` baseados em main antiga); esta iteração extraiu SÓ os 2 dir de componente (imports batem com atoms/molecules/tokens atuais), descartou as branches stale e reancorou sobre a main pós-ENG-236. `SessionList` §7.2 (superfície facilitadora, dígitos permitidos): `<ul aria-label="sessões">` de cards brancos, rótulo de status via `data-status` em-progresso/concluida, título/slug·projeto·última-modificação (string pré-formatada — organismo não faz aritmética de data), relance de progresso por `StepperStation` (nada de `<ol>` vazio sem estações), UMA ação (Retomar→onResume / Abrir→onOpen) nomeada por composição verbo+título via span visually-hidden dentro do botão (nome acessível composto, APG; card nunca clicável, sem interativos aninhados); `SessionStationGlance` espelha o `StepperStationView` do shell SEM importar a camada de wiring. `ArtifactCards` §8.8/redesign §6.7: 3 `DocumentCard` com filenames+nomes-humanos PT-BR verbatim dos protótipos ("As decisões de vocês"/"O mapa das contas"/"A conversa sobre o sentido"), flip Baixar→baixado delegado ao DocumentCard, `onDownload(kind)` fan-out, chip de celebração "documentos salvos — nada saiu deste computador" numa live region PERSISTENTE (`role=status` montado desde o 1º render, só o texto entra com os 3 baixados — WCAG 4.1.3/ARIA22), pop sob guarda reduced-motion, digit-free §9.2. Barrel intocado (congelado; consumido por caminho direto pelas páginas Dashboard/ENG-245 e Export/ENG-246). Sem browser test (nenhum `Necklace`/geometria). 14 dom (8 session-list + 6 artifact-cards); typecheck/lint(0 erros)/depcruise/test(758)/golden(18/18) verdes) · PR #TBD
- 2026-07-10 17:45 · ENG-246 · merged (ui/pages — estação Export/Completion "Guardar os documentos" §8.8/§10.5. `Export({store,sessionId,saveBytes})`: colar inteiro `transportOnly` sob "A história está inteira no colar.", os 3 `DocumentCard` do organism `ArtifactCards` (import direto, barrel congelado) e a conclusão que constrói o trio UMA vez via os builders de contracts (`serializeArtifact(buildManifesto/buildRetorno)`+`buildMapReport`) e o entrega opaco ao `SessionStore.complete` byte-idêntico — `getArtifacts` serve os MESMOS bytes ao dashboard; os downloads reusam o `triple` memoizado (nenhum rebuild). Máquina de fases `loading→edit|saved` a partir de `store.get(id).status`: concluída→revisão c/ "Destravar para editar"→`store.reopen` (re-materializa na próxima conclusão); em_progresso→"Concluir e guardar os documentos" (desabilitado sem `whole.confirmed`). Gates 1:1 da referência: retorno exige `retornoExportStatus.canExport`→cópia exata "Confirme o colar antes de exportar.", aviso `semFim` literal "N frase(s) ainda sem fim travado.", manifesto no-op silencioso sem grade. `saveBytes(filename,bytes)` = fronteira de download injetável (default Blob/anchor; testes injetam spy p/ afirmar identidade); filenames via `retorno/manifesto/relatorioFilename(slug)` prefixados. A `SessionMeta` fora do domínio (exigida por `toSessionDto`) é recuperada relendo o DTO persistido no mount (try/catch de IO → default). Superfície FACILITADORA (§7.2) → contagens permitidas. Estação ainda NÃO alcançável no shell (`reachable=false`, sem `Mode` export) — ligar roteamento+passar store/id é edição de SHELL fora do Scope → follow-up. 7 testes dom (conclusão byte-idêntica; identidade download↔dashboard + Baixar→baixado; bloqueio do retorno; contagem semFim; reopen→re-materializa; título+colar; creme+reduced-motion); typecheck/lint(0 erros)/depcruise/test(765)/test:browser(19)/golden(18/18) verdes) · PR #TBD
