# Falha de install auto-explicativa quando o Node local está abaixo do piso
# (>= 22.12 — jsdom 29 usa require(esm); Vite 8 exige o mesmo). Sem isso, o
# sintoma é um ERR_REQUIRE_ESM críptico dentro do jsdom.
engine-strict=true
